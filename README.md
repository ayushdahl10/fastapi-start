#
docker compose build
docker compose up 

#navigate inside app folder
#RUN
python cmd
  -load_roles
  -load_permissions
  -update_permissions
  -create_superuser
  -create_user #optional if required to create admin user
  
